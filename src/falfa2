falfa2() {
    if [ $# -lt 1 ]; then
        echo "Usage: falfa2 <subs/domains_list>"
        return
    fi

    ffuf -u https://FUZZ/BUZZ -c -fs 0 \
        -w "$1:FUZZ" \
        -w "$BFH_PATH/TXT/alpha3-4.txt:BUZZ" \
        -H "User-Agent: chrome"
}
